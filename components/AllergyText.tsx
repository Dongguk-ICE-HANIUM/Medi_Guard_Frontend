import { StyleSheet } from "react-native";

type AllergyInputProps = {
  text: string;
  onChange: (text: string) => void;
};

export default function AllergyInput({ text, onChange }: AllergyInputProps) {}

const styles = StyleSheet.create({});
